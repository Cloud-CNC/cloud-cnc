/**
 * @fileoverview TypeScript types
 */

/**
 * `package.json` `cloud-cnc` property type
 */
export interface PackageCloudCnc
{
  /**
   * What versions of Cloud CNC the plugin is compatible with
   */
  version: string;
}