/**
 * @fileoverview TypeScript types
 */

/**
 * Plugin options
 */
export interface Options
{
  /**
   * Truncate the commit hash to the first 7 characters
   * @default true
   */
  truncateHash: boolean;
}